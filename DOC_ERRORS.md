# 文档错误清单

**检查时间**: 2026-01-25
**检查范围**: docs/core.md, docs/design.md
**检查依据**: 以实际实现为准
**状态**: ✅ 已全部修复

---

## ✅ 已修复错误

### ✅ 错误1: cond 使用方括号语法 (core.md 第35-38行) - 已修复

**位置**: docs/core.md 第35-38行

**原错误内容**:
```lisp
(cond
  [test1 expr1]
  [test2 expr2]
  [else default])
```

**已修复为**:
```lisp
(condb
  test1 expr1
  test2 expr2
  else default)
```

---

### ✅ 错误2: let 使用方括号绑定 (core.md 第51-52行) - 已修复

**位置**: docs/core.md 第51-52行

**原错误内容**:
```lisp
(let ([x 1] [y 2])
  (+ x y))
```

**已修复为**:
```lisp
(let ((x 1) (y 2))
  (+ x y))
```

---

### ✅ 错误3: let 使用方括号绑定 (core.md 第325-328行) - 已修复

**位置**: docs/core.md 第325-328行

**原错误内容**:
```lisp
(let ([count 0])
  (lambda ()
    (set! count (+ count 1))
    count))
```

**已修复为**:
```lisp
(let ((count 0))
  (lambda ()
    (set! count (+ count 1))
    count))
```

---

### ✅ 错误4: match 使用方括号和箭头语法 (design.md 第44行) - 已修复

**位置**: docs/design.md 第44行

**原错误内容**:
```lisp
| `cond` | `match` | `(match lst [[a b] -> a])` | 模式匹配（扩展） |
```

**已修复为**:
```lisp
| `cond` | `condb`/`match` | `(condb test1 expr1 test2 expr2)` | 条件表达式/模式匹配 |
```

---

## 原始错误说明（供参考）

以下内容保留用于参考，说明错误的原理和测试方法。

### ❌ 错误1: cond 使用方括号语法 (第35-38行)

**位置**: docs/core.md 第35-38行

**错误内容**:
```lisp
(cond
  [test1 expr1]
  [test2 expr2]
  [else default])
```

**问题**:
1. `cond` 特殊形式**未实现**
2. 已实现的是 `condb` (增强版条件表达式)
3. 即使是 `condb`，也不使用方括号语法

**正确语法**:
```lisp
;; 使用 condb (增强版)
(condb
  test1 expr1
  test2 expr2
  else default)
```

**或者用 match 替代**:
```lisp
(match true
  test1 expr1
  test2 expr2
  _ default)
```

---

### ❌ 错误2: let 使用方括号绑定 (第51-52行)

**位置**: docs/core.md 第51-52行

**错误内容**:
```lisp
(let ([x 1] [y 2])
  (+ x y))
```

**问题**: 方括号绑定的语法**不支持**，返回 nil

**正确语法**:
```lisp
;; 使用圆括号
(let ((x 1) (y 2))
  (+ x y))
```

**测试结果**:
```lisp
(let ([x 1] [y 2]) (+ x y))  ; => nil (错误)
(let ((x 1) (y 2)) (+ x y))  ; => 3 (正确)
```

---

### ❌ 错误3: let 使用方括号绑定 (第325-328行)

**位置**: docs/core.md 第325-328行

**错误内容**:
```lisp
(let ([count 0])
  (lambda ()
    (set! count (+ count 1))
    count))
```

**问题**: 方括号绑定的语法**不支持**

**正确语法**:
```lisp
(let ((count 0))
  (lambda ()
    (set! count (+ count 1))
    count))
```

---

## docs/design.md 错误

### ❌ 错误4: match 使用方括号和箭头语法 (第44行)

**位置**: docs/design.md 第44行

**错误内容**:
```lisp
(match lst [[a b] -> a])
```

**问题**:
1. match 不使用方括号 `[pattern result]`
2. match 不使用箭头 `->` 语法

**正确语法**:
```lisp
;; 直接写模式，然后结果
(match lst
  (a b) a)
```

**测试结果**:
```lisp
(define lst '(1 2))
(match lst [[a b] -> a])  ; => nil (错误)
(match lst (a b) a)       ; => 1 (正确)
```

---

### ❌ 错误5: match 使用箭头语法 (第130行)

**位置**: docs/design.md 第130行

**错误内容**:
```lisp
(match (future.get(timeout: 5000))
  (Some result) (println "结果:" result)
  (None) (println "超时"))
```

**问题**: 箭头语法 `->` 在 match 中**不支持**

**正确语法**:
```lisp
(match (future.get(timeout: 5000))
  (Some result) (println "结果:" result)
  (None) (println "超时"))
```

---

## docs/core.md 建议更新

### 📝 建议1: 将 cond 改为 condb

由于 `cond` 未实现，但 `condb` (增强版) 已实现，建议将所有 `cond` 示例改为 `condb`。

**更新位置**:
- 第35-38行: cond 示例

---

## docs/design.md 正确的语法

### ✅ 正确1: let 向量解构 (第60-61行)

```lisp
(let [[x y & rest] data]
  (println x y rest))
```

**测试结果**: ✅ **正常工作** `(1 2 (3 4 5))`

---

### ✅ 正确2: let 嵌套向量解构 (第64-65行)

```lisp
(let [[[a b] c] nested]
  (println a b c))
```

**测试结果**: ✅ **正常工作** `(1 2 3)`

---

## 总结

### 需要修复的错误数量: 5个

| # | 文件 | 行数 | 错误类型 | 优先级 |
|---|------|------|----------|--------|
| 1 | core.md | 35-38 | cond 未实现 | 🔴 高 |
| 2 | core.md | 51-52 | let 方括号绑定 | 🔴 高 |
| 3 | core.md | 325-328 | let 方括号绑定 | 🔴 高 |
| 4 | design.md | 44 | match 方括号和箭头 | 🔴 高 |
| 5 | design.md | 130 | match 箭头语法 | 🟡 中 |

### 修复建议

1. **高优先级**: 修复 core.md 中的 `cond` 和 `let` 方括号语法
2. **高优先级**: 修复 design.md 中的 `match` 方括号和箭头语法
3. **建议**: 在文档中添加 "已实现特性" 和 "未实现特性" 的说明

---

**相关文件**:
- UNSUPPORTED_FEATURES.md: 已记录的不支持功能
- docs/syntax/02-modern.md: 已修正的现代语法文档
