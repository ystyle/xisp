;; std.math 核心函数
;; 定义基础数学运算函数

(define (add x y)
  "返回两个数的和"
  (+ x y))

(define (subtract x y)
  "返回两个数的差"
  (- x y))

(define (multiply x y)
  "返回两个数的积"
  (* x y))

(define (divide x y)
  "返回两个数的商"
  (/ x y))

(define (square x)
  "返回数的平方"
  (* x x))

(define (cube x)
  "返回数的立方"
  (* x x x))
